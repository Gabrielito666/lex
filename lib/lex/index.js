export { createElement } from "#lib/create-element";
export { Fragment } from "#lib/fragment";
export { mount, startClient } from "#lib/mount";
export { useRef } from "#lib/ref";
export { State, useState } from "#lib/state";
export { useClient } from "#lib/use-client";

import { createElement } from "#lib/create-element";
import { Fragment } from "#lib/fragment";
import { mount, startClient } from "#lib/mount";
import { useRef } from "#lib/ref";
import { State, useState } from "#lib/state";
import { useClient } from "#lib/use-client";

const Lex = {
  createElement,
  Fragment,
  mount,
  useRef,
  State,
  useState,
  useClient,
  startClient,
};

export default Lex;
